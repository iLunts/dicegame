$(document).ready(function () {

	var countChildTop = $('.timeline__list--top .timeline__item').length;
	var screenWidth = $(document).width();
	// var addPaddingLeft = $(document).width();
	var itemWidth = $('.timeline__item').width();
	
	console.log('width: ' + itemWidth);

});
